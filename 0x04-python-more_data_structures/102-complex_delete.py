#!/usr/bin/python3
def complex_delete(a_dictionary, value):
    if a_dictionary is not None and key in a_dictionary:
        for dkey in a_dictionary:
            if value == a_dictionary[dkey]:
                del a_dictionary[dkey]
        return (a_dictionary)
